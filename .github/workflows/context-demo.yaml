name: Context variables demo
on: push
jobs:
  context-demo:
    runs-on: ubuntu-latest
    steps:
      - name: Use context variables
        run: |
          echo "This workflow is triggered by ${{ github.actor }}"
          echo "the repo is ${{ github.repository }}"
      - name: Print all var under github
        run: "${{ toJson(github) }}"
