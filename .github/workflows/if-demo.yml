name: If-demo
on:
  push:
    branches:
      - main
      - develop

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Run for main step
        if: github.ref_name == 'main'
        run: echo "Running for main step"
      - name: Run this step
        run: echo "Running this step"
