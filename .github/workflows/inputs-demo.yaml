name: Input Demo
on: 
  workflow_dispatch:
    inputs:
      environment:
        description: Choose the environments to deploy
        required: true
        default: dev
jobs:
  input-demo:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy the app
        run: echo " Deploy the applications to ${{ inputs.environment }}"
